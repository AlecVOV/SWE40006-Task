services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    environment:
      - FRONTEND_ORIGIN=http://localhost

  frontend:
    build:
      context: ./frontend
      args:
        VITE_BACKEND_URL: http://localhost:8000
    ports:
      - "8080:8080"
    depends_on:
      - api
